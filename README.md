How to reproduce:

1. `npm i`
2. `gulp serve --locale=nl-nl`
3. Add Hello World web part to a page (local or hosted WB). 
4. Check that label contains "NL" - meaning nl-nl locale is used. It works for SPFx 1.11, but not for SPFx 1.12